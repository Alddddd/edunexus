[phases.setup]
nixPkgs = ["php83", "php83Extensions.gmp", "nodejs_22", "npm", "composer"]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm install --no-audit --no-fund"
]

[phases.build]
cmds = [
  "npm run build"
]

[variables]
NODE_ENV = "production"
